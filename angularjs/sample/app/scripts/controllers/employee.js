'use strict';

mainModule.controller('employeeController',
  ["$scope", "$localStorage", "$http", "$window", '$rootScope', '$timeout', 'dialogs', 'spinnerService',
    function ($scope, $localStorage, $http, $window, $rootScope, $timeout, dialogs, spinnerService) {


    }]);
